<?php

function mardown_parser($raw_markdown) {
	// parse markdown into html
	// wrap in markdown block
	// transforms it into blocks!
	// <!-- release-notes/convert-to-block {} -->
	// <!-- /release-notes/convert-to-block -->
	return;
}


// const Block = ({ attributes, clientId }) => {
// 	useEffect(() => {
// 		const blocks = wp.blocks.rawHandler(attributes.html);
// 		dispatch('core/block-editor').replaceBlocks([clientId], blocks);
// 	}, [attributes]);

// 	return null;
// }
